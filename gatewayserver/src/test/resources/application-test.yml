spring:
  config:
    activate:
      on-profile: test
  cloud:
    config:
      enabled: false # Disabilita il client di Spring Cloud Config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8070/eureka/
