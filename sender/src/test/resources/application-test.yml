spring:
  config:
    activate:
      on-profile: test

  # --- Disable Cloud features for tests ---
  cloud:
    config:
      enabled: false
    stream:
      enabled: false

# --- Eureka Configuration for Tests ---
eureka:
  client:
    enabled: false